import React from 'react';
import Section from '@/components/privacyPolicy/section/Section';

const Changes = () => {
  return (
    <Section title="Cambios a la Política de Privacidad">
      Nos reservamos el derecho de modificar esta política. La última actualización fue el [fecha].
    </Section>
  );
};

export default Changes;
