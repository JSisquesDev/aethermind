import React from 'react';
import Section from '@/components/privacyPolicy/section/Section';

const ExternalLinks = () => {
  return (
    <Section title="Enlaces a Otros Sitios">
      Nuestra política de privacidad no se aplica a otros sitios a los que puedas acceder desde el nuestro.
    </Section>
  );
};

export default ExternalLinks;
